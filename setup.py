from setuptools import setup

setup(
    name="new-albums",
    version="0.22.0",
    install_requires=[
        "spotipy",
        "python-decouple",
        "python_dateutil",
        "rich",
        "pycountry",
    ],
)
